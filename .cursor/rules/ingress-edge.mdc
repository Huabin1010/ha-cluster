---
description: 入口机轻量分流、调度池 Relay、域名与 Nginx 分层
alwaysApply: true
---

# 入口与流量拓扑

- **所有业务域名 DNS 只指向入口机**（公网 IP）；算力 worker **无公网业务入站**。
- **入口机（Edge）**：常在线、尽量轻 — EasyTier Hub + OpenResty/Nginx（TLS + 按域名转发），**不跑用户 Workspace**。
- **调度池（Relay）**：`10.129.129.2`–`.9`，接收入口 `proxy_pass`，经 EasyTier 转到 worker `fabric_ip:host_port`。
- **算力节点**：`10.129.129.10`+；仅虚网可达。

```
公网 → 入口(:443, TLS) → upstream 负载均衡 → Relay 池 → EasyTier → Worker → Workspace
```

## 实现约束

- 入口配置 **不随域名数量线性增长**：`*.apps` 共用一个 `server` + `upstream ha_relay_pool`；按 Host 查后端在 **Relay** 完成。
- 控制台/API 走入口公网 443，**不依赖** overlay 存活；overlay 断 ≠ 删 Workspace。
- Ingress 审批：`pending_approval` → owner/admin → 同步 Relay 路由表。详见 `23-ingress-fabric-topology.md`、`25-ingress-portforward-dns.md`。
- **域名分级**：`*.apps.<platform>` **公共域**免审直领；**特殊域**（自有域等）须审批。DNS 一键配置支持 **腾讯云 DNSPod / 阿里云解析**（凭证不进 Git）。
- **暴露类型**：HTTP/HTTPS 用反向代理（Ingress）；非 HTTP 用 **TCP 端口转发**（`PortForward`，待实现）。用户在工作区自配，不经 SSH。
- 改 `internal/ingress/` 或 OpenResty 模板时保持 **入口 / Relay / worker** 三层边界。
