---
description: 项目、工作区（机器）、审批与一行安装 — 产品模型
alwaysApply: true
---

# 项目与工作区（用户口中的「机器」）

- **Project**：协作边界（成员、预算、多台机器）；**非成员看不见项目**。成员可有 `ssh_access`（能否连）与 `ssh_mode`（只读/读写），见 `30-project-member-access.md`。
- **Workspace**：用户申请的隔离 Linux 环境（独立文件系统），不是 PVE/KVM 整机。
- **Node**：经 EasyTier 纳管的宿主机；用户不独占 Node，按套餐 **硬占用** 切片（Ledger）。运行时 **Incus + ha-agent**，PVE 仅可作测试用 Node 仿真。
- 术语与流程以 `docs/implementation/22-project-machine-concepts.md` 为准。

## 成员与角色

- 添加同事：**搜索用户 + 角色下拉**（admin/developer/viewer）立即加入（`04-collaboration.md` §4.1）。
- owner 可将项目负责人 **转让给** 项目内其他成员；转让后 **原 owner 降为 `developer`**。
- 审批权委派：设 **`admin`** 即可批创建/升配/降配/SSH；**不能** 委派销毁终审。

## 审批

| 操作 | 项目 admin | 平台超级管理员 |
|------|------------|----------------|
| 创建 / 升配 / 降配 | 终审 | 可强制 |
| **销毁机器** | **仅初审** | **必须终审**（可委派给 `platform_ops` 等，见 29 §8） |

- `viewer` 不能申请机器。owner/admin 直建可免审；升配与降配一律 pending。
- 销毁：`destroy_requested` → 项目通过 → `destroy_pending_platform` → 平台终审。

## 一行命令

- **纳管 Node（设备）**：仅 `platform_admin`；一行 `curl install.sh | join`（见 `node-install-depot.mdc`、`platform-governance.mdc`）。
- **工作区**：用户不手工装环境；审批后平台自动 Launch → 控制台提供 **一行 `ssh …@bastion -t <workspace-id>`**。
- **运行环境**：`container`（Docker+SSH，Incus，默认）或 `k8s`（Namespace+Quota）。创建时 **自动注入**（Docker/SSH 公钥；k8s 则 ns+kubeconfig）。详见 `27-project-monitoring-and-runtimes.md`。
- **项目监控**：列表展示每台机器 CPU/内存/磁盘快照；详情页 **近期曲线**（agent 上报 workspace 指标，待实现）。
