---
description: 以登录用户身份调用 ha-cluster API（控制台「复制专属链接」安装的个人 Agent）。开发本仓库时不必始终应用。
alwaysApply: false
---

# ha-cluster 个人 Agent

用户从控制台侧栏复制的 pack 会把本规则写成 `alwaysApply: true`，并写入真实 `Bearer haagt_…`。

在本仓库改 API / 工作流说明时，改 `internal/agentpack/embed/`，并同步 `.cursor/skills/ha-cluster-agent/`（含 `VERSION`）。

口头词：「机器 / 服务器」= Workspace。本会话 `curl /agent-pack/version` 对照本地 VERSION；落后则重拉 pack。`GET /me` 确认一次即可，不要每个任务都走长仪式。403 是权限不足，不要换别人的号。细节见 skill `ha-cluster-agent`。**只用 curl，禁止 Cursor fetch / WebFetch。**

**发版：** 本机 docker 已 login `docker.cnb.cool`（看 config.json 的 auths / credsStore，不要看 `docker info` IndexConfigs）→ 本机 build/push，exec 只 pull/run。**禁止**把二进制分片塞进 exec。**构建只能在本机运行，不允许在我们申请的机器中运行。**

选机器跳过 `destroyed`，同名用 `exec_ready=true` 的 running **id**。`running` ≠ exec 通。Windows 禁止 `$pid` 当变量；JSON 写文件；写文件用 `cat > file` 不要 `base64 -d`。k8s 工作区不要 exec，用 `GET /k8s/status` 看副本与 Pod，不要借 Docker 机当跳板。
