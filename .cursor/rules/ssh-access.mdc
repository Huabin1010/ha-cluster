---
description: SSH 公钥、Bastion 跳板、连接权申请与审批
alwaysApply: true
---

# SSH 极速连接与权限

细则见 `docs/implementation/24-ssh-access-and-approval.md`、`30-project-member-access.md`。

- 用户在 **个人设置** 登记 SSH 公钥；至少一把公钥才能过 Bastion。
- **非项目成员看不见该项目**（列表无、直链 404）。
- 工作区 `running` 且有权时，控制台提供 **网页终端** 与 **一键 SSH**；网页终端走 JWT + WebSocket，不要求本机私钥。
- 无 SSH 连接权时只显示申请入口，不开放终端。

## 谁能连（须全部满足）

1. **成员身份**：`memberships` 存在；非成员无项目访问权。
2. **项目层** `ssh_access`：`granted`（`none`/`pending`/`revoked` 不可连；`viewer` 默认 `none`，可申请）。
3. **SSH 模式** `ssh_mode`：`read_write`（默认）或 `read_only`（可登录但不可改工作区文件）。
4. **工作区层**：`shared` vs `private` ACL。
5. owner/admin 默认可连；`platform_admin` 破窗须审计。

## 实现约束

- `SSHTarget` 校验成员 + `ssh_access` + ACL；只读模式用 ro 账号或 `ha-shell-ro` ForceCommand。
- 公钥热同步到 Incus `authorized_keys`（Launch + `sync-keys`）。
- 改 `internal/bastion/`、`internal/service` 时保持公钥鉴权 + 审计 `ssh.allow` / `ssh.deny`。
