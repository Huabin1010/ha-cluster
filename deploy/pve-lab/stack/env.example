# 复制为 stack.env 后按需修改（勿提交 stack.env）

# 控制面监听
API_PORT=8080
POSTGRES_PASSWORD=ha

# ha-api
HA_JWT_SECRET=dev-insecure-change-me-please-32b
HA_ADMIN_PASSWORD=123456qq
HA_NODE_TOKEN=ha-test-node-token-2026
HA_INTERNAL_TOKEN=internal-dev
HA_RUNTIME=remote
HA_AGENT_VIA_LAN=1
HA_BASTION_DIRECT=1
HA_BASTION_HOST=192.168.1.66
HA_BASTION_PORT=8099
HA_SEED=1

# EasyTier Hub（install-hub.sh 写入 /etc/ha-cluster/easytier-ha.env）
HUB_LAN_IP=192.168.1.8
HA_ET_NET=ha-cluster-easytier
HA_ET_SECRET=generate-on-install
HA_ET_IPV4=10.129.129.1/24
HA_ET_LISTEN_PORT=15010

# Worker bootstrap 见 deploy/pve-lab/lab.env（勿写进 docker compose env）
