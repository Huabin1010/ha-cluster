# 工作流（用 {{USERNAME}} 的身份）

基址 `{{API_BASE}}`，头 `Authorization: Bearer {{TOKEN}}`。**只用 curl**，禁止 Cursor fetch / WebFetch。Windows 用 `curl.exe`，JSON 写文件后 `--data-binary @file`，**禁止 `$pid` 当变量名**。

本会话：`GET /agent-pack/version` 一次、`GET /me` 一次。不要每个任务都把项目/机器/入口/仓库/exec 探活走一遍。

## 0. 更新已有服务（默认这条）

用户说「加功能 / 更新 / 重新上线」时：

```
本机已 login CNB？  （看 ~/.docker/config.json 的 auths / credsStore，或 docker images）
  ├─ 是 → 本机 docker build && docker push
  └─ 否 → 仍本机构建；请用户 docker login docker.cnb.cool
           （不要改去 exec 传二进制；不要 docker info 的 IndexConfigs 判断没登录）
GET /workspaces?project_id=     → 跳过 destroyed；同名优先 exec_ready=true 的 running id
                                 exec_ready=true 不必再 uname；false 则 POST /start 或换一台
POST /workspaces/{id}/exec      → 仅当 exec_ready 缺省时用 {"command":"uname -a"} 探活
  ├─ 502 EXEC_UNAVAILABLE      → POST /start，再探；仍失败换一台
  └─ 通 → exec 里 docker pull && docker run/compose up
POST /exec                      → docker ps -a && docker logs <name>
  └─ Restarting / permission denied → chown 数据目录后再起
域名还没有才 POST /ingress/shared
```

**构建只能在本机运行。** `exec` 只做 pull / run / logs / 短排障。

## 1. 开通一台 2c2g（默认 Compose）

**默认这条。** 不写 `runtime` 或写 `container`：Docker + SSH，机子里用 `docker compose` 部署。用户没提 k8s / kubectl / Namespace 时不要走下面的 Kubernetes。

```
GET /projects                          → 记下 project_id；若 purpose 为空，先 PATCH 补上再动手
POST /projects
  {"name":"办公零食","slug":"office-snacks","purpose":"办公室零食柜与内部协作"}
PATCH /projects/{id}                   → 仅当旧项目 purpose 为空：{"purpose":"一句话用途"}；看到 409 PURPOSE_REQUIRED 就停。变量名用 $proj，禁止 $pid
GET /projects/{id}/members             → 看自己的 role / ssh_access
POST /projects/{id}/workspaces
  {"name":"dev-1","plan":"2c2g","arch":"amd64","visibility":"shared","runtime":"container"}
```

仅当用户明确要 Kubernetes（需节点带 `k3s` / `k8s` / `both` 标签，否则 `409 INSUFFICIENT_CAPACITY`）。**审批链与 Docker 相同，不是免审。**

```
POST /projects/{id}/workspaces
  {"name":"k8s-1","plan":"nano","arch":"amd64","visibility":"shared","runtime":"k8s"}
```

- 自己是 owner/admin / `platform_admin`：直接开通，轮询 `GET /workspaces/{id}` 直到 `running`（通常 <90s）。
- 自己是 developer：`status=requested`，请项目 owner/admin：
  `POST /workspaces/{id}/approve`（Docker 与 Kubernetes 相同）
- viewer：403，停止。

容量不够：`409 INSUFFICIENT_CAPACITY`。先让用户停/销毁闲置机器，或换小套餐。

## 2. 停 / 启

仅 `running` / `stopped` / `fabric_degraded` 可停启。`destroy_*` 状态会 `400`。

```
POST /workspaces/{id}/stop
POST /workspaces/{id}/start
```

`running` 但 exec 502 时，先 `start` 再探活，不要认定机器坏了就换字段重试。

## 3. 进机器（只走 HTTP 执行）

**不要本机 `ssh`、不要 `ssh-config`、不要 8099。** 进机器只有：

```
POST /workspaces/{id}/exec
  {"command":"uname -a && whoami && pwd"}
```

返回 `{exit_code, stdout, stderr, via:"http-ssh"}`。命令失败时 HTTP 仍是 200，看 `exit_code`。HTTP 502 且 `error=EXEC_UNAVAILABLE`：SSH 不通（handshake / connection reset），不是 JSON 填错。

**构建只能在本机运行，不允许在我们申请的机器中运行。** `exec` 只做运行与排障（`docker pull`、`docker compose up`、`docker logs`）。禁止在机器里 `docker build` / `docker compose build` / `npm run build` / `go build`。**禁止**把可执行文件 base64 分片塞进 exec（nginx 约 60s 会 504，Agent 终端长脚本会 Aborted）。

写小文件：平台已把 `stdin_b64` **解码后**写入 stdin，用 `cat` 接收原文：

```
POST /workspaces/{id}/exec
  {"command":"cat > /tmp/note.txt","stdin_b64":"<BASE64>"}
```

不要写 `base64 -d`（会 `invalid input`）。只传 KB 级文本。

`timeout_sec` 默认 60，最大 300；前面网关仍可能 60s 超时，所以不要把 build 塞进一次 HTTP。只读 SSH（`ssh_mode=read_only`）会 403。**Kubernetes 工作区 exec 返回 400**，改走下一节 apply + status。

## 3b. Kubernetes：apply / 状态 / kubeconfig

k8s 机器 **没有 SSH**。`POST /exec` 一定 400。**不要**把 kubeconfig 写到同一节点上的 Docker 机器（即使它 `exec_ready=true`）再跑 `kubectl` / python 探活——审计会看起来像「apply 在 A、探活全在 B」。

```
POST /workspaces/{id}/k8s/apply
  {"yaml":"apiVersion: apps/v1\nkind: Deployment\n..."}
GET  /workspaces/{id}/k8s/status          ← 看滚动更新是否真正起 Pod
GET  /workspaces/{id}/k8s/resources       ← 扁平列表（含 ready / labels）
GET  /workspaces/{id}/kubeconfig          → {kubeconfig}  仅当用户要在本机 kubectl
DELETE /workspaces/{id}/k8s/resources?kind=Deployment&name=web
```

`GET /k8s/status` 顶层就是快照，不要再包一层 `data`：

- `summary`：deployments / ready_deployments / pods / running_pods / pending_pods / failed_pods / warnings
- `quota`：`project-quota` 的 hard / used（`requests.cpu` `requests.memory` `pods`）
- `deployments[]`：`replicas` `ready_replicas` `updated_replicas` `rolling` `strategy` `max_unavailable` `max_surge` `labels`（含 `release`）`missing_requests`
- `replica_sets[]`：两代同时 desired>0 就是正在 RollingUpdate
- `pods[]`：`phase` `ready` `restarts` `labels` `reason` `message`
- `events[]` / `warnings[]`：FailedCreate、配额拦截会写在这里
- `resources[]`：与 `GET /k8s/resources` 相同的扁平对象

apply 之后立刻打 status。若 `warnings` 含 `project-quota` / `exceeded quota` / `missing_requests`：补容器 `resources.requests.cpu` 与 `resources.requests.memory` 再 apply，不要换机器重试。示例：

```
resources:
  requests:
    cpu: 50m
    memory: 64Mi
```

清单会被强制改写到工作区 Namespace；`ClusterRole` / `Node` 等集群级对象会被拒。viewer 只能 GET resources / status。需要域名再走现有 Ingress API。

开通和 apply 时会把平台 **auto_inject** 的 Docker 仓库（线上 CNB 登录）写成 Secret `ha-registry`，并挂到 Namespace 的 default ServiceAccount；Deployment 也会自动补 `imagePullSecrets`。不要在清单里再写一遍 docker login。

若 `GET kubeconfig` 里 `server` 是 `https://k8s.invalid`，或 apply 返回 **503 `K8S_UNAVAILABLE`**，说明控制面没有真实 k3s，不要反复换字段重试。本机若到不了虚网 apiserver，也**不要**改去 Docker 机器上打 6443——继续用 `GET /k8s/status`。`fabric_degraded` 时仍应先 apply（JSON `{yaml}`），不要对 k8s 工作区 `POST /start`（那会去找不存在的 Incus 实例）。

`GET /workspaces/{id}/connection` 含 `exec_example`。`ssh_access` 不是 `granted`（且不是 owner/admin）时 exec 为 403。让用户：

```
POST /projects/{pid}/ssh-access-request
```

owner/admin 批准：

```
POST /projects/{pid}/members/{uid}/ssh-access/approve
```

或 `PUT /projects/{pid}/members/{uid}` `{"ssh_access":"granted"}`。

config 里 `Host ha-<8位>-et` 走 EasyTier 虚网（`10.129.129.253`），不占 42 公网带宽。

## 4. 拉人 / 邀请 / 转让

```
POST /projects/{pid}/members
  {"username":"alice","role":"developer"}     → 201；已存在 409

PUT /projects/{pid}/members/{uid}
  {"role":"admin"}                            → owner 才能提到 admin

POST /projects/{pid}/invitations
  {"email":"bob@x.com","role":"viewer"}       → 把 token 给对方
POST /invitations/accept
  {"token":"..."}                             → 必须是邀请邮箱对应的账号

POST /projects/{pid}/transfer-ownership
  {"new_owner_user_id":"<uuid>"}              → 仅当前 owner；自己降为 developer
```

踢人：`DELETE /projects/{pid}/members/{uid}`（不能踢 owner）。developer 不能拉人。

## 5. 公共域 `*.apps`

```
GET /ingress/meta                      → shared_zones / zones，记下 zone_id（领域名时才打）
POST /workspaces/{id}/ingress/shared
  {"zone_id":"...","mode":"random","port":8088}
POST /workspaces/{id}/ingress/shared
  {"zone_id":"...","mode":"custom","prefix":"myapp","port":8090,"confirm_second_port":true}
```

同一主机第二端口必须确认。保留前缀 `admin` / `api` / `auth` / `console` 会被拒。特殊自有域走 `POST /workspaces/{id}/ingress`，要审批。非 `running` 不能 claim。

## 6. 推镜像 / 部署（默认 Compose + CNB）

按第 0 节决策树。本机：

```
docker build -t docker.cnb.cool/<组织>/<仓库>:<标签> .
docker push docker.cnb.cool/<组织>/<仓库>:<标签>
```

申请的机器里（`POST /workspaces/{id}/exec`）只允许：

```
docker pull docker.cnb.cool/<组织>/<仓库>:<标签>
docker compose up -d
docker ps -a && docker logs <name>
```

数据目录若挂到 `/root/...` 且镜像是非 root `USER`，先 `chown` 再跑，否则会 `permission denied` + `Restarting`。

- 控制台 https://cnb.cool 建仓库后，镜像路径就是 `docker.cnb.cool/…`。
- 不确定仓库名才 `GET /registries/preferred`。
- 平台若已把 CNB 凭据自动注入工作区，容器内可直接 **pull**；push 在本机做。
- 部署跑起来并 `docker logs` 正常后再按上一节领取 `*.apps` 域名。

## 7. 升配 / 销毁

```
POST /workspaces/{id}/resize
  {"cpu_milli":4000,"mem_bytes":4294967296,"disk_bytes":21474836480}
POST /workspaces/{id}/resize/approve      # owner/admin
POST /workspaces/{id}/resize/reject
```

销毁（两段）：

```
POST /workspaces/{id}/destroy-request              # 申请人
POST /workspaces/{id}/destroy-request/approve      # 项目 owner/admin 初审
POST /admin/dangerous-approvals/{id}/approve       # 仅平台角色终审
```

`DELETE /workspaces/{id}` 对普通用户等于发起销毁申请；`platform_admin` 才会直接删。

## 8. 平台（仅 staff）

```
POST /admin/join-tokens          {"use_lan_depot":false}
GET  /nodes
PATCH /nodes/{id}                {"machine_type":"self","remark":"...","tags":["gpu"]}
GET  /capacity
GET  /audit-logs
GET  /admin/docker-registries
GET  /admin/ingress-domains
GET  /admin/tls-certs
POST /admin/tls-certs/{id}/issue
POST /users/batch
POST /users/{id}/suspend
```

join token 不要发给普通项目成员。目标机：`curl install.sh | sudo bash -s join --token 'ha://join/...'`。

## 9. HTTPS 证书（仅 platform_admin）

公共域（如 `*.apps.cl.qzsyzn.com`）由平台用 ACME **DNS-01** 签发通配符证书，到期前 30 天自动续期，并挂到入口 443。用户领取的 `*.apps` 子域出厂即 HTTPS，不必自己申请证书。

```
GET /admin/tls-certs
POST /admin/tls-certs/{id}/issue          # 立即签发或续期
PATCH /admin/tls-certs/{id}               {"auto_renew":true}
```

- 列表不含 `cert_pem` / `key_pem`。
- `issue` 可能要等 DNS 生效（约 1–3 分钟）；失败看返回的 `error` / `cert.last_error`。
- 控制台站点 `cl.qzsyzn.com` 一般仍由宝塔管。不要对 `*.cl.qzsyzn.com` 随便点签发，以免覆盖控制台证书。
- 普通用户 403 是正常的。
