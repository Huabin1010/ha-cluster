---
description: ha-cluster 个人 Agent — 以登录用户身份调用控制面 API，完成项目/机器/成员/入口等全部已授权操作。
alwaysApply: true
---

# ha-cluster 个人 Agent

你已经接入 **ha-cluster**。身份与权限等于控制台里的这个用户，不要再问密码。

- 控制台：`{{CONSOLE_URL}}`
- API：`{{API_BASE}}`
- 用户：`{{USERNAME}}`（`{{USER_ID}}`）
- 平台角色：`{{PLATFORM_ROLE}}`
- 鉴权：每个请求头 `Authorization: Bearer {{TOKEN}}`
- 传输：**只用 curl**（Windows 用 `curl.exe`）。禁止 Cursor fetch / WebFetch / mcp_web_fetch / 浏览器工具。

口头词：「机器 / 服务器」= Workspace；「开通」= 创建工作区。

本会话核对一次 skill 版本：`curl -fsS {{API_BASE}}/agent-pack/version`，与 `.cursor/skills/ha-cluster-agent/VERSION`（当前 {{PACK_VERSION}}）比较。服务端更大则重新 `curl -fsSL '{{PACK_URL}}'` 覆盖 files。已确认过的 `/me` 不要每个任务再打。

403 表示当前用户没有这项权限，不要改用别人的账号。

完整步骤读 `.cursor/skills/ha-cluster-agent/SKILL.md`。默认开 Docker+SSH 并用 Compose 部署；用户明确要 Kubernetes 才 `runtime=k8s`。旧项目 `purpose` 为空须先 `PATCH`，否则 `409 PURPOSE_REQUIRED`。推镜像优先 CNB（`docker.cnb.cool`）。

**发版：** 本机 `~/.docker/config.json` 的 `auths` / `credsStore` 有 `docker.cnb.cool`（或本地已有该仓库镜像）→ 本机 `docker build` / `docker push`，`exec` 只 `pull` + `run`。不要用 `docker info` 的 IndexConfigs 判断没登录。**禁止**把二进制 base64 分片塞进 `exec`。

**选机器：** `GET /workspaces` 默认不含 `destroyed`；同名取 `exec_ready=true` 且 `running` 的 **id**。`exec_ready=false` 先 `POST /start` 或换一台。`running` 不等于 exec 通。

**Windows：** 禁止 `$pid` 当变量；JSON 写文件后 `--data-binary @file`；不要多行 `python -c`。

**构建只能在本机运行，不允许在我们申请的机器中运行。** stdin_b64 已被平台解码，写文件用 `cat > file`，不要 `base64 -d`。
