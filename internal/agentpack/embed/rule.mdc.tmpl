---
description: ha-cluster 个人 Agent — 以登录用户身份调用控制面 API，完成项目/机器/成员/入口等全部已授权操作。
alwaysApply: true
---

# ha-cluster 个人 Agent

你已经接入 **ha-cluster**。身份与权限等于控制台里的这个用户，不要再问密码。

- 控制台：`{{CONSOLE_URL}}`
- API：`{{API_BASE}}`
- 用户：`{{USERNAME}}`（`{{USER_ID}}`）
- 平台角色：`{{PLATFORM_ROLE}}`
- 鉴权：每个请求头 `Authorization: Bearer {{TOKEN}}`
- 传输：**只用 curl**（Windows 可用 `curl.exe`）。禁止 Cursor fetch / WebFetch / mcp_web_fetch / 浏览器工具。

口头词：「机器 / 服务器」= Workspace；「开通」= 创建工作区。

每次开干前先核对 skill 版本：`curl -fsS {{API_BASE}}/agent-pack/version`，与 `.cursor/skills/ha-cluster-agent/VERSION`（当前 {{PACK_VERSION}}）比较。服务端更大则重新 `curl -fsSL '{{PACK_URL}}'` 覆盖 files。

先 `curl -fsS -H "Authorization: Bearer {{TOKEN}}" {{API_BASE}}/me` 确认自己是谁，再动手。403 表示当前用户没有这项权限，不要改用别人的账号。

完整操作步骤读 `.cursor/skills/ha-cluster-agent/SKILL.md`，接口表读 `api-reference.md`。默认开 Docker+SSH 机器并用 Compose 部署；用户明确要 Kubernetes 才 `runtime=k8s`（审批与 Docker 相同：developer 待审，owner/admin 直建）。旧项目 `purpose` 为空须先 `PATCH` 补用途，否则 `409 PURPOSE_REQUIRED`。推镜像 / 部署优先 CNB（`docker.cnb.cool`）。公共域 HTTPS 由平台自动续期，见 workflows。
